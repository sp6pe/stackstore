//if(!req.session.cart) req.session.cart = cart._id


//New session -- need to create new cart and link cart to session via cartid (!req.session.cart) cart.create() and 
			//req.session.cart = newcar._id


// if(req.session.cart), find the cart and use it for put, post, delete 


//if user logs in, set carts user to logged in user


//if user logs in, should be able to find old carts 



//Admin

//all users
//all products
//add/delete
//edit categories